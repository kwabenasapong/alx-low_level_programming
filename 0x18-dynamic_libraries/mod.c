#include <stdio.h>

/**
 * mod - function finds modulus of two numbers
 * @a: first number
 * @b: second number
 * Return: Sub of a and b
 */
int mod(int a, int b)
{
	int result;

	result = a % b;
	return (result);
}
