#include <stdio.h>

/**
 * mul - functions multiplies two numbers
 * @a: first number
 * @b: second number
 * Return: Sub of a and b
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
