#include <stdio.h>

/**
 * div - functions divides two numbers
 * @a: first number
 * @b: second number
 * Return: Sub of a and b
 */
int div(int a, int b)
{
	int result;

	result = a / b;
	return (result);
}
