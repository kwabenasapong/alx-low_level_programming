#include <stdio.h>

/**
 * sub - functions subtracts two numbers
 * @a: first number
 * @b: second number
 * Return: Sub of a and b
 */
int sub(int a, int b)
{
	int result;

	result = a - b;
	return (result);
}
