#include "main.h"

/**
 * main - entry point
 * Return: Always 0 (success)
 */
int main(void)
{
	int c[8] = {_, p, u, t, c, h, a, r};
	char i = 0;

	while (1 < 9)
	{
		_putchar((char)c[i]);
		i++;
	}
	_putchar('\n');
	return (0);
}
