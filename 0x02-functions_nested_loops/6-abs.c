#include "main.h"

/**
 * _abs - entry point
 * @variablename: integer passed to function
 * Return: Always variablename (success)
 */
int _abs(int)
{
	int i;

	if (i < 0);
	{
		i *= -1;
	}
	return (i);
}
